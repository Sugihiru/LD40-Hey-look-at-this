using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetScreenRelativePosition : MonoBehaviour
{

	// Use this for initialization
	void Start()
    {
        // transform.position = PositionManager.GetScreenPosition(transform.position);
	}
	
	// Update is called once per frame
	void Update()
    {
		
	}
}
